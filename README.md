Create app_keys.py with

```
ttn_app_info = [
    dict(
        host="<app_router_url>",
        username="<app_name>",
        password="<app_key>"
    ),
]
sms_info = dict(
    phone="+<target_phone>"
)
```
